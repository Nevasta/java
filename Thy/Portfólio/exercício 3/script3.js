function Cal() {
    let n1 = 0
    let n2 = 0
    let res = window.prompt("Digite")
    res = res.toUpperCase()

    if (res === "ADIÇÃO" || res === "ADICAO") {
        console.log("ola")
    }

    else if (res === "SUBTRAÇÃO" || res === "SUBTRACAO") {

    }

    else if (res === "EQUAÇÃO" || res === "EQUACAO") {

    }

    else if (res === "MULTIPLICAÇÃO" || res === "MULTIPLICACAO") {

    }
    
}